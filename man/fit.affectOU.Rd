% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit.affectOU}
\alias{fit.affectOU}
\title{Fit Ornstein-Uhlenbeck model to data}
\usage{
\method{fit}{affectOU}(object, data, times = NULL, method = "mle", start = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{\link{affectOU}}.}

\item{data}{A numeric vector of observed affect values. Must contain at
least 2 finite observations.}

\item{times}{A numeric vector of observation times, the same length as
\code{data} and strictly increasing, since observations are ordered in time
with no repeated time points. If \code{NULL}, unit spacing is assumed.
If \code{NULL}, defaults to equally spaced times: 0, 1, 2, …}

\item{method}{A single string naming the estimation method.
Currently only \code{"mle"} (i.e., maximum likelihood estimation)
is supported.}

\item{start}{Optional named vector of starting values for parameters:
\code{theta}, \code{mu}, and \code{gamma}. If \code{NULL}, reasonable defaults are chosen
based on the data. Specifically, \code{theta} is initialized using the
empirical lag-1 autocorrelation, \code{mu} is initialized to the sample mean,
and \code{gamma} is initialized based on the stationary variance estimate.}

\item{...}{Additional arguments (unused)}
}
\value{
An object of class \code{\link[=fit.affectOU]{fit_affectOU}}, containing:
\describe{
\item{parameters}{Named list of fitted parameter estimates.}
\item{se}{Named list of standard errors for parameters.}
\item{fitted_values}{Conditional means under the fitted model.}
\item{residuals}{Vector of residuals (\code{data - fitted_values}).}
\item{log_likelihood}{Maximized log-likelihood value.}
\item{rmse}{Root Mean Squared Error of the fitted values.}
\item{convergence}{Optimizer convergence code.}
\item{start}{List of starting values used by the optimizer.}
\item{nobs}{Number of observations.}
\item{data}{The observed data used for fitting.}
\item{times}{The observation times used.}
\item{method}{Estimation method used (e.g., \code{"mle"}).}
\item{model}{The original \code{affectOU} model used for fitting.}
}
}
\description{
Estimates OU parameters using the exact discrete-time transition density
via maximum likelihood. Currently limited to univariate (1D) models.
}
\examples{
model <- affectOU(theta = 0.5, mu = 0, gamma = 1)
sim <- simulate(model, stop = 1000, dt = 0.01, save_at = 0.01)
data <- as.data.frame(sim)
fitted <- fit(model, data = data$value, times = data$time)
print(fitted)
plot(fitted)
}
\concept{fit}
