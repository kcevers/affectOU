% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{confint.fit_affectOU}
\alias{confint.fit_affectOU}
\title{Confidence intervals for fitted OU affect model}
\usage{
\method{confint}{fit_affectOU}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{\link[=fit.affectOU]{fit_affectOU}}.}

\item{parm}{Optional character vector naming parameters of the fitted
model to include: any of \code{theta}, \code{mu}, and \code{gamma}.
If missing, all parameters are included.}

\item{level}{Confidence level for intervals: a number between 0 and 1
(default 0.95).}

\item{...}{Additional arguments (unused).}
}
\value{
Matrix of confidence intervals with columns for lower and upper bounds.
}
\description{
Confidence intervals for fitted OU affect model
}
\examples{
model <- affectOU(theta = 0.5, mu = 0, gamma = 1)
sim <- simulate(model, stop = 500, dt = 0.01, save_at = 0.1)
data <- as.data.frame(sim)
fitted <- fit(model, data = data$value, times = data$time)
confint(fitted)
}
\concept{fit}
