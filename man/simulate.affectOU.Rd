% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate.affectOU}
\alias{simulate.affectOU}
\title{Simulate from Ornstein-Uhlenbeck process}
\usage{
\method{simulate}{affectOU}(
  object,
  nsim = 1,
  seed = NULL,
  initial = NULL,
  dt = 0.01,
  stop = 100,
  save_at = dt,
  ...
)
}
\arguments{
\item{object}{An \code{affectOU} model object.}

\item{nsim}{How many independent trajectories to simulate. A whole number of
at least 1.}

\item{seed}{The random seed, so a simulation can be reproduced. A whole
number, or \code{NULL} to leave the random state alone.}

\item{initial}{The affect value each trajectory starts from. A single
number, or a vector with one element per dimension. If \code{NULL}, defaults
to a draw from the stationary distribution (for stable systems) or the
attractor location \code{mu} (for non-stable systems).}

\item{dt}{The time step the simulation advances by, for the Euler-Maruyama
discretization (smaller = more accurate). Must be larger than 0.}

\item{stop}{How long the simulated period lasts, in time units. Must be
larger than 0.}

\item{save_at}{The time interval at which simulated data is saved, in time units; used to
linearly interpolate results. Useful for reducing output size. Must be at
least \code{dt}, because states are only computed every \code{dt} time units, and at
most \code{stop}, or nothing would be recorded.}

\item{...}{Additional arguments (unused).}
}
\value{
A model object of class \code{\link[=simulate.affectOU]{simulate_affectOU}} containing:
\describe{
\item{model}{The original \code{affectOU} model object used for simulation.}
\item{data}{A 3D array with dimensions (time x ndim x nsim) containing the simulated trajectories.}
\item{times}{A vector of time points corresponding to the rows of the \code{data} array.}
\item{nsim}{The number of simulations performed.}
\item{dt}{The time step used for the Euler-Maruyama discretization.}
\item{stop}{The total simulation time.}
\item{save_at}{The time interval at which simulated data was saved.}
\item{seed}{The random seed used for simulation (if any).}
}
}
\description{
Generates a trajectory from the Ornstein-Uhlenbeck process using
Euler-Maruyama discretization. Handles both univariate and multivariate models.
}
\examples{
model <- affectOU(ndim = 2)
sim <- simulate(model, nsim = 2)
plot(sim)
summary(sim)
head(sim)

# Specify initial state
sim <- simulate(model, initial = c(1, -1))
plot(sim)

# Simulate for a longer time with coarser saving interval
sim <- simulate(model, stop = 500, save_at = 10)
plot(sim)

}
\concept{simulate}
